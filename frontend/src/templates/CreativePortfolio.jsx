import { formatDate, getFontSize, getSpacing } from './templateUtils';
const T = ({ data, theme }) => {const pi=data?.personalInfo||{};const color=theme?.primaryColor||'#f97316';const font=theme?.fontFamily||'Poppins';const fs=getFontSize(theme?.fontSize);const sp=getSpacing(theme?.spacing);return(<div style={{fontFamily:font,color:'#1f2937',padding:sp.padding,background:'white',minHeight:'1056px'}}><div style={{display:'flex',gap:'20px',alignItems:'flex-end',marginBottom:sp.section,paddingBottom:'16px',borderBottom:`3px solid ${color}`}}><div style={{width:'70px',height:'70px',borderRadius:'12px',background:`linear-gradient(135deg,${color},${color}aa)`,display:'flex',alignItems:'center',justifyContent:'center',fontSize:'28px',fontWeight:800,color:'white'}}>{(pi.fullName||'Y')[0]}</div><div><h1 style={{fontSize:fs.name,fontWeight:800,margin:0}}>{pi.fullName||'Your Name'}</h1><div style={{fontSize:fs.body,color:'#6b7280'}}>{[pi.email,pi.phone,pi.location].filter(Boolean).join(' • ')}</div></div></div>{pi.summary&&<div style={{marginBottom:sp.section,padding:'16px',background:`${color}08`,borderRadius:'10px'}}><p style={{fontSize:fs.body,lineHeight:1.6,margin:0}}>{pi.summary}</p></div>}{data?.experience?.length>0&&<div style={{marginBottom:sp.section}}><h2 style={{fontSize:fs.section,fontWeight:700,color,marginBottom:sp.item}}>Portfolio Experience</h2>{data.experience.map((e,i)=>(<div key={i} style={{marginBottom:'14px',padding:'12px',background:'#fafafa',borderRadius:'8px'}}><div style={{display:'flex',justifyContent:'space-between'}}><strong style={{fontSize:fs.title}}>{e.position}</strong><span style={{fontSize:'10px',color:'#9ca3af'}}>{formatDate(e.startDate)}–{e.current?'Present':formatDate(e.endDate)}</span></div><div style={{fontSize:fs.body,color,fontWeight:500}}>{e.company}</div>{e.bullets?.filter(b=>b).map((b,bi)=>(<p key={bi} style={{fontSize:fs.body,margin:'3px 0',color:'#4b5563'}}>→ {b}</p>))}</div>))}</div>}{data?.education?.length>0&&<div style={{marginBottom:sp.section}}><h2 style={{fontSize:fs.section,fontWeight:700,color,marginBottom:sp.item}}>Education</h2>{data.education.map((e,i)=>(<div key={i} style={{marginBottom:sp.item}}><strong style={{fontSize:fs.title}}>{e.degree} {e.field&&`in ${e.field}`}</strong><div style={{fontSize:fs.body,color:'#6b7280'}}>{e.institution}</div></div>))}</div>}{data?.skills?.length>0&&<div style={{marginBottom:sp.section}}><h2 style={{fontSize:fs.section,fontWeight:700,color,marginBottom:sp.item}}>Toolkit</h2><div style={{display:'flex',flexWrap:'wrap',gap:'6px'}}>{data.skills.map((s,i)=>(<span key={i} style={{fontSize:'11px',padding:'5px 14px',borderRadius:'8px',background:`${color}12`,color,fontWeight:600,border:`1px solid ${color}30`}}>{s.name}</span>))}</div></div>}{data?.projects?.length>0&&<div style={{marginBottom:sp.section}}><h2 style={{fontSize:fs.section,fontWeight:700,color,marginBottom:sp.item}}>Featured Projects</h2><div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:'8px'}}>{data.projects.map((p,i)=>(<div key={i} style={{padding:'12px',background:'#fafafa',borderRadius:'8px'}}><strong style={{fontSize:fs.title}}>{p.name}</strong>{p.technologies&&<div style={{fontSize:'10px',color:color,marginTop:'2px'}}>{p.technologies}</div>}{p.description&&<p style={{fontSize:'10px',margin:'4px 0 0',color:'#6b7280'}}>{p.description}</p>}</div>))}</div></div>}{data?.certifications?.length>0&&<div><h2 style={{fontSize:fs.section,fontWeight:700,color,marginBottom:sp.item}}>Certifications</h2>{data.certifications.map((c,i)=>(<div key={i} style={{fontSize:fs.body,marginBottom:'3px'}}><strong>{c.name}</strong> – {c.issuer}</div>))}</div>}</div>)};
export default T;
